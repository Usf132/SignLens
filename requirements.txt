# Core runtime dependencies for the SignLens Streamlit app.
# (Trimmed from a full `pip freeze` — the previous requirements.txt mixed
# these with ~130 unrelated notebook/dev packages such as ipykernel, jedi,
# and debugpy. Re-add those separately, e.g. in a requirements-dev.txt,
# if you need to run the training notebooks.)

streamlit==1.60.0
opencv-contrib-python-headless==4.11.0.86
numpy==1.26.4
plotly==5.24.1
toml==0.10.2
python-dotenv==1.2.3
elevenlabs==1.59.0
mediapipe==0.10.21
tensorflow==2.17.1
ultralytics==8.3.0
tornado==6.4.1
streamlit-camera-input-live==0.2.0
